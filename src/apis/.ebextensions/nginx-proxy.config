option_settings:
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx

container_commands:
  01_adjust_permissions:
    command: "sudo chmod -R 750 /etc/pki/tls/map-api"
  02_reload_nginx:
    command: "service nginx reload"
  
