/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },

  useFileSystemPublicRoutes: false
}

module.exports = {
  reactStrictMode: true,
};
